import s from '../sass/layouts/info.module.scss';
const Info = () => {
  return (
    <section className={s.kontrola}>
      <div className={`${s.container} ${s.container__kontrola} `}></div>
    </section>
  );
};
export default Info;
